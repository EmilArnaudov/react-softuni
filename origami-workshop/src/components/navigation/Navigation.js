import styles from './Navigation.module.css'

export default function Navigation() {
    return (
        <>
            <nav className={styles.navBar} >
                <a href="#" className={styles.navBarLogo}>HOOKED.</a>
            </nav>        
        </>
    )
}